<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="xmapirest1.Views.TodoItemPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Name" Text="{Binding Path= Name}" ></Entry>
            <Entry Placeholder="Notes" Text="{Binding Path= Notes}"></Entry>
            <Label Text="Done:"></Label>
            <Switch IsToggled="{Binding Path= Done}"></Switch>

            <Button x:Name="save" Text="Save" Clicked="save_Clicked" ></Button>
            <Button x:Name="del" Text="Delete" Clicked="del_Clicked" ></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>